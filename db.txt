--240710
ALTER TABLE `receipt_voucher` CHANGE `status` `is_tally_synced` TINYINT NOT NULL DEFAULT '0' COMMENT '1:Tally Updated';

ALTER TABLE `sales_order` CHANGE `update_status` `is_tally_synced` TINYINT NOT NULL DEFAULT '0' COMMENT '0:int, 1:updateTally, 2:failed';

ALTER TABLE `voucher_types` ADD `voucher_prefix` VARCHAR(20) NOT NULL AFTER `voucher_type`, ADD `start_number` INT NOT NULL AFTER `voucher_prefix`;

ALTER TABLE `voucher_types` ADD `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP AFTER `status`, ADD `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP AFTER `created_at`;

-- 260614
ALTER TABLE `sales` ADD `previous_balance` DECIMAL(11,2) NOT NULL DEFAULT '0.00' AFTER `paid_amount`;